<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shu747.androidcourse">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/funny"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/funny"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GuideActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.homework1Activity"></activity>
        <activity android:name=".activity.Homework2Activity"></activity>
    </application>

</manifest>